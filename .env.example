# PostgreSQL Configuration (for docker-compose.local.yml)
# These values are used when running local PostgreSQL via Docker Compose
POSTGRES_DB=openrun
POSTGRES_USER=your-db-username
POSTGRES_PASSWORD=your-db-password
