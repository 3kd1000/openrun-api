apiVersion: v1
kind: Service
metadata:
  name: openrun-admin-{{ .Values.environment }}-service
  namespace: openrun
  labels:
    app: openrun-admin
    env: {{ .Values.environment }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
    protocol: TCP
    name: http
  selector:
    app: openrun-admin
    env: {{ .Values.environment }}
