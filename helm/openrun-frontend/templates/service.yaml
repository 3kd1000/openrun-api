apiVersion: v1
kind: Service
metadata:
  name: openrun-frontend-{{ .Values.environment }}-service
  namespace: openrun
  labels:
    app: openrun-frontend
    env: {{ .Values.environment }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.port }}
    protocol: TCP
    name: http
  selector:
    app: openrun-frontend
    env: {{ .Values.environment }}
